# Copy this file to .env.local and fill in the values you need.
# All variables here are optional — the app runs fine without them,
# just with the corresponding feature disabled.

# GitHub Personal Access Token (read:user scope).
# Powers the GitHub contributions heatmap on the home page.
# Without it, the heatmap section is simply not rendered.
GITHUB_TOKEN=
